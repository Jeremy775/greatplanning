<form method="post" action="{{ path('app_cda_delete', {'id': cda.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cda.id) }}">
    <button class="btn">Delete</button>
</form>
