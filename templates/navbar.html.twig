{% if app.user %}
	
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="collapse navbar-collapse" id="navbarNav">
		<ul class="navbar-nav">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('planning') }}">Planning</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('app_cours_index') }}">Cours</a>
			</li>
			{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin') }}">Admin</a>
				</li>
			{% endif %}
			{% if app.user %}
				
				{% if is_granted('ROLE_USER') or is_granted('ROLE_FORMATEUR') %}
					
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_user_show', {'id': app.user.id}) }}">
							{{ app.user.prenom }}
						</a>
					</li>

				{% endif %}

				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
				</li>
				
			{% endif %}
		</ul>
	</div>
	
</nav>

{% endif %}